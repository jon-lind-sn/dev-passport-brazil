name: Build, test, and promote to prod (basic auth) - PR validation

on:
  pull_request:
    branches:
      - main

jobs:
  validate:
    uses: ./.github/workflows/test-and-validate.yml
    secrets: inherit
